const BUDGET_TYPES = ["low", "medium", "high"];

const PRIORITY_TYPES = ["speed", "quality", "cost"];

const TOOL_CATEGORIES = [
  "chat",
  "coding",
  "research",
  "image",
  "video",
  "agent",
  "workflow",
];

const PRICING_TYPES = ["free", "freemium", "paid", "api"];

const ORG_SIZES = ["startup", "small", "medium", "enterprise"];

const POLICY_ACTIONS = ["remove", "replace", "reduce_budget"];

module.exports = {
  BUDGET_TYPES,
  PRIORITY_TYPES,
  TOOL_CATEGORIES,
  PRICING_TYPES,
  ORG_SIZES,
  POLICY_ACTIONS,
};
